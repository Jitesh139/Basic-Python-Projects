from pypdf import PdfWriter

merger = PdfWriter()
pdfs=[]
n=int(input("how many pdfs you want to merge "))
for i in range(0,n):
    name=input(f"enter the {i+1} name of the pdf ")
    pdfs.append(name)
for pdf in pdfs:
    merger.append(pdf)

merger.write("merged-pdf.pdf")
merger.close()
